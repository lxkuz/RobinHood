// Generated by CoffeeScript 1.10.0
(function() {
  var GameChart;

  GameChart = (function() {
    function GameChart(el) {
      var data, j, len, obj, p1Arr, p2Arr, xArr;
      this.el = el;
      this.$el = $(this.el);
      data = this.$el.data('chart');
      p1Arr = [];
      xArr = [];
      p2Arr = [];
      for (j = 0, len = data.length; j < len; j++) {
        obj = data[j];
        p1Arr.push(obj.p1);
        xArr.push(obj.x);
        p2Arr.push(obj.p2);
      }
      this.$el.highcharts({
        title: {
          text: 'Значения коэффициентов по ходу игры',
          x: -20
        },
        yAxis: {
          plotLines: [
            {
              value: 0,
              width: 1,
              color: '#808080'
            }
          ]
        },
        legend: {
          layout: 'vertical',
          align: 'right',
          verticalAlign: 'middle',
          borderWidth: 0
        },
        series: [
          {
            name: 'p1',
            data: p1Arr
          }, {
            name: 'x',
            data: xArr
          }, {
            name: 'p2',
            data: p2Arr
          }
        ]
      });
    }

    return GameChart;

  })();

  $(function() {
    return $('.game-chart').each(function(i, obj) {
      return new GameChart(obj);
    });
  });

}).call(this);

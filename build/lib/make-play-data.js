// Generated by CoffeeScript 1.10.0
(function() {
  var WavesRobot, _, bookmakerKoef, bookmakerSafeKoef, delta, gamesCount, makePlayData, money, profitKoef, simpleBet, stepsCount;

  _ = require('underscore');

  stepsCount = 100;

  bookmakerKoef = 0.5;

  bookmakerSafeKoef = 0.95;

  delta = 0.2;

  simpleBet = 10;

  money = 100;

  gamesCount = 100;

  profitKoef = 2.5;

  WavesRobot = require('../robots/simple-waves');

  makePlayData = function(robot) {
    var el, gameData, i, len, res;
    gameData = JSON.parse(robot.get('gameData'));
    res = {};
    robot = new WavesRobot({
      bet: simpleBet,
      money: money,
      stepsCount: stepsCount,
      profitKoef: profitKoef
    });
    for (i = 0, len = gameData.length; i < len; i++) {
      el = gameData[i];
      robot.play(el);
    }
    res['simple_waves'] = robot.betsData;
    return res;
  };

  module.exports = makePlayData;

}).call(this);
